export enum Gender {
    MALE = 'male',
    FEMALE = 'female',
    UNKNOWN = 'unknown',
    BOTH = 'both',
}

export enum UserRole {
    ADMIN = 'admin',
    TEACHER = 'teacher',
    STUDENT = 'student',
}